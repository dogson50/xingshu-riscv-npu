# Pango backend

This directory will contain PG2L200H-specific APM MAC, memory, clock/reset and HMIC adapter implementations for Pango Design Suite.

The Vivado implementation remains the functional reference while the Pango backend is verified against the same cycle-level tests.